# 对上一轮结论的修正 + 下一步方案（含新实验证据）

本文档回应审阅意见。每条批评都做了**实测核验**，并给出修正后的准确表述；
末尾是基于新证据调整后的下一步方案。

---

## 一、四处批评的核验结果

### 1. `oracle_truth` 不是策略上界 —— **批评成立，我的表述错了**

代码事实（`analysis/stage2_proxy.py:235`）：权重由 `align_vec(priors, T_lab)` 在**已标注子集的真值核**上计算，
再作用到全部先验核上。而已标注扰动的真值响应本来就是任何主动学习策略可见的信号
（`stage2_proxy.py:227` 里 `T_lab = Y@Y.T` 的已标注块）。因此：

- 它既没有优化最终选样收益，也没有搜索最优权重序列 → **不能称为上界**；
- 它实际上是"用对齐度加权、且不含模型核"的**可部署方法**（与 `align` 的差别只是少了模型核），
  所以"连 oracle 都赢不了"这个论断**不成立**。

**修正后的表述**：在已测试的配置与代理环境中，几种加权方法没有显示出足够吸引人的收益，
因此**暂缓**该方向，但**不能**推出"不存在可赢空间"。

另外 `stage2_proxy.py:208` 的参考核 `ref_kernel` 由**全体真值响应**（含测试扰动）构建，
确实泄漏了测试集的几何结构 → 它只能作为**离线几何诊断**，不能解释为可部署预测器的泛化表现。
**推论**：代理上的增益量级同样不能直接迁移到 GEARS，不只是"排序需降级"。

> 若确实需要一个诚实的上界，应当：在每次决策点直接搜索"最优化最终评估指标"的权重（用测试真值），
> 明确标注为不可部署的诊断上界；而不是用可观测真值算出的对齐权重冒充上界。

### 2. stage4 的 97.5% 不是"学会了感知" —— **批评成立**

代码事实：`stage4_planner_learnability.py:45` 每次只换已标注子集，**测试集 `te` 与八个先验核始终不变**。
于是每个先验的 `align_te` 是常数，40 次抽样只重复 8 个不同的目标值。
"每子集 oracle = 全局最优固定先验"是**任务构造直接决定的**，不能证明真实上下文中适应没有价值。

CV 实现（`stage4:93`）确实是**留一行**（留出"某先验在某子集"这一条记录），
其余同一先验在别的子集上的记录仍在训练集里 → 不是"留一子集"或"留一先验"。

**修正后的表述**：该实验只说明——
**在这个固定的 K562 测试集上，可以从观测子集里识别出哪个先验的对齐度更高（r≈0.87）**。
这与我 stage1 的独立发现（先验质量的**排序**跨子集稳定、相关 0.895）一致，
但它是**单个任务内**的性质，不能外推到"跨上下文适应没有价值"。

同时**删除**"线性探针学不到 → LLM 也学不到"这一推论：线性模型失败不构成一般性不可学习证明，
LLM 还可以使用文本证据。

### 3. stage3 只证明了精度依赖细胞数 —— **批评成立，且我引用的数字有误导**

三个具体问题（全部核实）：

| 批评 | 代码事实 | 后果 |
|---|---|---|
| 参考与估计不独立 | `stage3:65-68`：`full` 用该扰动**全部**细胞，`est` 是其**子集** | r 随深度自然趋近 1，被高估 |
| 人群不具代表性 | `stage3:60`：只取 `>=250` 细胞的 60 个扰动 | 无法代表 <100 细胞的那 34% |
| 逻辑跳跃 | "更多细胞更准" ≠ "自适应分配优于均匀分配" | 若各扰动收益曲线相似，固定规则就够 |

另外我引用的 `0.82 → 0.92` 是**中位数**；均值是 `0.751 → 0.851`。原文档表格里两者都有，但我口头表述时
只用了中位数而未标注 → **这是我的表述问题，已在本轮全部改为以均值为准并标注两者**。

**新证据（stage6，修正后的重做）**：用**互不相交**的隐藏参考细胞（参考细胞从不进入估计，
且对照组也拆成两半分别用于估计与参考），34.1% 的扰动不足 100 细胞（censoring 显式报告）：

| depth | ALL: n | ALL: mean r | ALL: median r | HIGH(≥200): n | HIGH: mean r | HIGH: median r |
|---|---|---|---|---|---|---|
| 10 | 1971 | 0.249 | 0.195 | 186 | 0.438 | 0.476 |
| 25 | 1868 | 0.329 | 0.282 | 186 | 0.554 | 0.659 |
| 50 | 1622 | 0.387 | 0.360 | 186 | 0.617 | 0.749 |
| 100 | 820 | 0.488 | 0.504 | 186 | 0.668 | 0.811 |
| 150 | 348 | 0.640 | 0.776 | 186 | 0.690 | 0.840 |
| 200 | 186 | 0.703 | 0.854 | 186 | 0.703 | 0.854 |

**结论**：stage3 的 0.851（100 细胞均值）在独立参考下变成 **0.488（ALL）/ 0.668（HIGH）**；
高细胞子集系统性更容易，不能外推。测量噪声比我原来呈现的大得多。

### 4. 复现偏差的归因过于武断 —— **批评成立，其中 Random 一条我彻底错了**

**(a) Random**：我实测了当前环境（torch 2.14.0+cu130）：

```
fresh torch.Generator(), no manual_seed : [10, 0, 13, 18, ...]
after torch.manual_seed(999)            : [10, 0, 13, 18, ...]   ← 完全相同
after torch.manual_seed(12345)          : [10, 0, 13, 18, ...]   ← 完全相同
CUDA 上同样不变
```

并且**直接比对两个 Random run 的选择集**：run1 与 run2 在**全部 5 轮选的扰动完全相同**
（每轮 100/100 重合，例如 round1 都是 `ACD, ACOT12, ACTR10, ...`）。
→ `RandomSelectionMethod`（`bmdal_reg/bmdal/selection.py:233`）里的生成器**使用固定默认种子**，
`manual_seed` 对它无效。所以：

- **不是"未播种 → 不可复现"**，而是"**固定种子 → 所有 run 共用同一条抽样序列**"；
- run1(0.2825) 与 run2(0.2646) 的差异**全部来自训练种子**（`torch.manual_seed(run)`），不是抽样差异；
- 因此这个 Random 基线实际上只有**一条**选择序列、10 个训练种子，它的"随机性"被严重低估。

**(b) BALD**：我说"梯度特征在新显卡上不稳定"属于**未验证假设**。已核实的是
BALD 路径用 `cross_gene_out` 表征构建线性核并做训练条件化；两次 run 都低于老师 10-run 最小值
（0.1970/0.2184 vs min 0.2241）这一点是事实，但**原因未定位**。修正为"本机栈上存在未被解释的偏低"。

**(c) pops 核版本**：同样**不成立**——下载时间不同不构成证据；需要文件哈希/原文件比对才能判断，
目前**未验证**，应从结论里降级为待查项。

**(d) prior-only 的评估口径**：审阅意见完全正确。用**归一化梯形 AUC**（横轴为扰动数归一化）重算，
我复现了审阅给出的数字：

| 方法 | 本机 AUC | 老师 AUC | 差 |
|---|---|---|---|
| IterPert | 0.2448 | 0.2502 | −0.0054 |
| **IterPert-Prior-Only** | **0.2362** | — | **−0.0086 vs IterPert** |
| TypiClust | 0.2363 | 0.2291 | +0.0072 |
| Random | 0.2323 | 0.2074 | +0.0248 |
| ACS-FW / LCMD / BADGE | 0.2258 / 0.2228 / 0.2171 | 0.2274 / 0.2232 / 0.2266 | −0.002 / −0.000 / −0.010 |
| KMeans / BatchBALD / Core-Set / BALD | 0.2067 / 0.1995 / 0.1933 / 0.1833 | 0.2306 / 0.2133 / 0.2140 / 0.2018 | −0.024 / −0.014 / −0.021 / −0.019 |

→ **只看 @600 会漏掉信息**：prior-only 终点不退化，但整条曲线 AUC 低 0.0086。
以后一律以 AUC 为主指标、@600 为辅。

---

## 二、新实验：细胞分配到底有没有价值？（stage6 / 6b / 6c）

这是审阅建议的核心问题。我做了三步，前两步都发现**方法学陷阱**，第三步给出公平答案。

**stage6（初步模拟）**：C = 50 细胞/扰动，两阶段（25 或 100）vs 均匀 50 →
可观测特征规则 0.4317 **低于**均匀 0.4406；oracle 仅 +0.0053。

**stage6b（预算扫描）**：在 37.5/62.5/75 等中间预算上，混合策略看似大胜（+0.03~+0.045）——
**但这是离散化假象**：均匀臂只能用测量过的深度（25/50/100），在 37.5 时被迫浪费预算。
且"按增益排序"不等于"按阈值目标排序"，oracle 的构造本身也有问题。

**stage6c（公平比较：等成本 + 连续深度插值 + 逐目标 oracle）**：

| 目标 | oracle | 可观测特征 probe | est_norm | random |
|---|---|---|---|---|
| 平均可靠度 `mean_r`（N=820） | −0.0075 | −0.0022 | −0.0076 | −0.0117 |
| `frac r≥0.5` | −0.0177 | **+0.0102** | −0.0116 | −0.0079 |
| **`frac r≥0.7`** | **+0.0398** | **+0.0266** | +0.0000 | **−0.0280** |

（N=186 高细胞子集给出同方向结果：mean_r −0.0058；frac≥0.7 oracle +0.0457 / probe +0.0296 / random −0.0457）

**结论（这是本轮最重要的新知识）**：

1. **如果目标是"提高所有扰动的平均测量质量"→ 均匀分配最优**，连 oracle 都赢不了（负值）。
   "自适应分配"在这个目标下**没有价值**。
2. **如果目标是尾部型**（"让尽可能多的扰动被测到高可靠度"，接近发现型目标），
   则自适应分配有**真实 headroom（+0.04）**，且**约 2/3 可由在线可观测特征实现（+0.027）**，
   而随机分配会**变差**（−0.028）→ 说明这是真信号而非假象。
3. 在线特征的可预测性：留一扰动 R² ≈ 0.17，Spearman 0.21~0.24（弱但非零）；
   `half_split_r`（观测细胞两半一致性）是最有用的在线代理之一。

→ 审阅提出的目标**可以成立，但必须把终点改成尾部/发现型指标**，并且预期增益是**相对 3~4%** 量级，
不是数量级提升。

---

## 三、下一步方案（按审阅的五步，补上可执行规格与验收标准）

### 第 0 步（立刻可做，无需新算力）：口径修正
- 全部文档改为 **AUC 为主指标**；@600 仅作辅助；标注重叠的 mean/median。
- 删除/降级四条过度陈述（oracle、stage4 感知、stage3 外推、偏差归因）。
- 报告中显式标注：代理的参考核含测试真值几何 → **离线诊断**；代理增益不可迁移。

### 第 1 步：把多保真环境做可信（最高优先级工程）
规格（直接采纳审阅意见）：
- 每个扰动拆成 **measurement pool** 与 **hidden reference**（互不相交；对照组也拆两半）；
- 追加测量 = **揭示新细胞**（nested prefix），累计计费，禁止重复抽取同一细胞充数；
- 所有方法共享同一预设细胞顺序（减少比较噪声）；
- 术语统一：**细胞数量分配**（不说"深度"）；
- 容量现实：34.1% 扰动 <100 细胞 → 主实验限定在满足容量的子集并**显式声明偏向高产量扰动**，
  同时在全体上报告 censored 曲线；
- 第一版以细胞数计预算，之后再加"新扰动启动成本/批次成本/对照成本"。
- **验收**：给定 (pool, reference, 细胞顺序) 后，环境可复现地产生任意预算下的观测与成本账单。

### 第 2 步：用非 LLM 策略证明"动态决策值得学"
必须与**开发集选出的最佳固定策略**比较（不能只赢 Random）。基于新证据，终点要选对：
- 主终点建议：**固定总细胞预算下，达到 r≥τ 的扰动数**（τ 由预注册确定，如 0.7），
  次要终点：平均可靠度（预期均匀最优，作为对照）。
- 对照矩阵：固定浅测/固定深测/固定两阶段/不确定性驱动/效果量驱动/oracle（诊断上界）。
- **验收**：在 ≥5 个任务划分上，自适应策略在主终点稳定优于最佳固定策略（配对检验），
  否则**停止**该子问题、不要投入 planner。

### 第 3 步：确定主要科学目标 + 发现型指标
- 二选一（预注册）：(A) 固定细胞预算下提高未见扰动响应预测；
  (B) 固定细胞预算下发现更多对**预先指定通路**有真实响应的扰动。
- 若选 (B)：真实响应必须由 **hidden reference 细胞**定义，**不能**把 GO 注释同时当选样依据与成功标签。
- 只靠汇总 CSV 能算的：预测学习曲线、基于选择列表+注释的 GO 覆盖（仅作辅助覆盖指标）；
  DE 恢复/通路响应误差需要预测值或保存的模型 → 需要补存预测。

### 第 4 步：接入 planner，并检验它**真的用了反馈**
- 最小动作空间：{扩展新扰动 vs 追加已有扰动} × {给哪些候选分配多少细胞}。
- 对照：最佳非 LLM 自适应 / 同工具 prompt-only LLM / 训练后 LLM /
  **冻结初始计划不随反馈调整** / **打乱反馈对应关系**。
- **"真实反馈 > 打乱反馈"是关键证据**，用于区分"靠预训练知识挑基因"与"真从本次实验反馈学习"。
- 先例存在（BioDiscoveryAgent），贡献必须比"接上 LLM 选样"更具体。

### 第 5 步：把迁移提升为核心（不是可选）
- 最终至少要有**一个未见任务或生物背景**：K562 内部通路留出（早期）→ 人工子空间位移（机制）→
  真实跨细胞背景（结论级，需要 RPE1 数据；本机当前缺失，需先获取）。
- 不同随机种子只是重复实验，**不是**迁移任务。

---

## 四、修正后的近期目标（建议写入 exp2）

> **在独立 hidden reference 细胞评估、相同总细胞预算下，证明反馈驱动的"扰动 + 细胞分配"策略，
> 能在尾部型终点（达到 r≥τ 的扰动数）上稳定超过开发集选出的最佳固定分配策略。**

配套口径：
- 主指标 = AUC（预算-收益曲线），并预注册 τ；
- 每个结论 ≥5 个任务划分、配对比较、报 seed/划分方差；
- planner 只在第 2 步通过验收后才启动。

## 五、尚未验证 / 风险清单
1. pops/biogpt 偏离原因未定位（需核文件哈希比对）。
2. BALD 偏低原因未定位（非"梯度特征不稳定"的证据）。
3. 代理指标与 GEARS 的一致性未验证 → 所有代理结论仅作探索。
4. 细胞分配增益目前只在"测量保真度"目标上量化；
   它与**下游 GEARS 预测/发现质量**的关系尚未测（这是第 1、2 步之后必须补的一环）。
5. 全部 stage6 结论基于回顾性重采样，不含真实新细胞获取成本。

---

# 第二轮更正（stage7：严格隔离 + 精确整数深度）

## 1. stage6c 的 oracle 确实错了 —— 已验证并撤回相关结论

`stage6c_fair_allocation.py:89` 的 `for name, kind in OBJ.items():` 中 `kind` 是 **lambda**，
`kind == 'mean_r'` **恒为假** → 三个目标全部落进 `else` 分支（都用了 r≥0.7 的边际排序）。
因此 stage6c 表格里"三个 oracle"实际是同一个（错配的）oracle，
**"平均质量目标下均匀分配最优（oracle 为负）"的结论作废**。

同时撤回两处措辞：
- 25/100 两档策略族的最优方案输给均匀分配 → 只能否定**这个受限策略族**，不能证明均匀分配全局最优；
- "在线特征实现了约 2/3 收益"→ 原数字既非纯留出、也不是预算曲线的 AUC，且 `+0.0266` 是
  **2.66 个百分点**，不是相对 2.66%。

## 2. stage7：重做的可信评估（本轮新证据）

修复了四点：**① 去掉插值与累计最大值修正**（r(d) 在 **每个整数深度**用固定共享细胞顺序精确测量）；
**② 严格开发/测试隔离**（probe 只在 574 个 dev 扰动上拟合，分配与评分只在 246 个 test 扰动上）；
**③ 真实整数细胞与严格计费**；**④ 3 个独立参考划分**并报告跨划分标准差。
人群限制显式报告：pool≥100 的扰动占 39.9%（820/2057）。

测试池（N=246）上的预算曲线 AUC（budget 25→90 细胞/扰动）：

| 目标 | uniform | oracle | probe(dev拟合) | est_norm | half_split_r | random |
|---|---|---|---|---|---|---|
| mean_r | 0.4193 | **+0.0077** | −0.0078 | −0.0102 | −0.0110 | −0.0166 |
| frac r≥0.5 | 0.4174 | **+0.0219** | −0.0059 | −0.0180 | −0.0190 | −0.0282 |
| frac r≥0.7 | 0.2473 | +0.0216 | **+0.0206** | +0.0192 | +0.0176 | −0.0132 |

跨参考划分 std：0.001–0.009（probe 在 frac≥0.7 上 +0.0206 ± 0.005）。

**修正后的结论**：
1. **true oracle 在三个目标上都有正收益**（+0.008 ~ +0.022）→ 决策空间**确实存在**，
   不是"均匀分配全局最优"。
2. **但可部署规则的表现依赖目标**：mean_r 与 frac r≥0.5 上，所有在线规则仍**输给均匀分配**
   （−0.006 ~ −0.019），说明现有在线特征不足以捕捉那部分 headroom；
   只有 **frac r≥0.7 这类尾部目标**上，probe 能拿到 oracle 的 ~95%（+0.0206 vs +0.0216）。
3. `random` 在所有目标上一致为负 → 上述尾部收益是**真信号**。
4. **不把 τ=0.7 当作预注册终点**：它是本轮已看过的探索结果，必须在**未使用的新测试任务**上验证。

## 3. 尚未完成（第 1 步的剩余部分）

stage7 已具备：隐藏参考、共享细胞顺序、真实整数追加、严格计费（报告实际成本）、
dev/test 隔离、参考稳定性。**仍缺**：
- 显式成本模型（新扰动启动成本、批次成本、对照成本）——目前只按细胞数计费；
- 可被 planner 调用的 `reset/step` 接口（当前是批量评估脚本）；
- **下游生物学终点**（发现型指标）尚未接入；
- 参考本身只有 20–80 细胞，是有噪声的"真值"，需多划分评估（已做 3 个，可增加）。

---

# 第三轮：MFBudgetEnv 交付 + 再次修正

## 1. 交付：`analysis/mf_budget_env.py`（含 18 项测试，全部 PASS）

| 验收要求 | 实现 |
|---|---|
| `reset()` 固定任务划分/观测顺序/预算 | 构造参数 + `seed`/`control_split_seed` 全部写入 `config` |
| `step(alloc)` 检查容量与预算、只揭示新细胞 | `_validate()` 先校验（越界/超容量/超预算 → 抛错，**状态不变**），再 `_apply()` |
| 观测接口返回已购买表达与统计量 | `Obs`：`delta`（伪 bulk Δ 向量）、`delta_norm`、`half_split_r`、`cell_sd`、`n_observed`、`n_capacity`、`remaining_budget`、`cost_so_far` |
| 独立评分器 | `Scorer` 独立对象，持有 hidden reference；`Obs` 白名单字段测试确保参考不可达 |
| 成本模型 | `CostModel(cell, startup, batch, control, charge_initial)`，主实验 `cell=1` 其余 `0`；初始分配**计费**（`charge_initial=True`），startup 只在首次测量时收一次，control 只收一次 |
| 回放记录 | `env.log`：步号、动作、**实际揭示的细胞 ID**、成本明细、累计成本、seed |

测试覆盖：不重复揭示、超容量拒绝且状态不变、超预算拒绝且状态不变、未知扰动/负数拒绝、
同 seed 精确回放（细胞 ID 与 Δ 逐位相同）、不同 seed 顺序不同、`Obs` 字段白名单、
参考不出现在 `Obs`、成本明细与语义（startup 不重复计费、control 只计一次）、
`finalize` 不超支、容量封顶。

**未做（仍在清单上）**：下游生物学终点尚未接入；成本系数的敏感性分析未做
（不虚构真实价格）；参考细胞仅 20–80 个、本身有噪声。

## 2. 阈值目标的 oracle 错配 —— 已修正并撤回

stage7 对所有目标都用 `r100 − r25` 排序，对"达标比例"是错配的。
现改为**按目标各自的边际排序**（`oracle_2lvl(mean_r)` / `oracle_2lvl(frac_ge_0.7)`），
并明确它是"**两档（25→100）策略族内、针对指定目标的最优分配**"，不是所有策略的上界。
→ **撤回"probe 拿到 oracle 约 95%"**。

## 3. stage8：在环境上跑验收（真实 K562，严格计费）

协议：dev/test 一次划分（probe 只在 dev 拟合）；test 上分配/计费/评分；
`env.step()` 真实整数细胞；剩余预算由环境的固定 round-robin 规则花掉 →
**每条策略实际成本完全相同**（`billed == actual` 全表为 True）；3 个划分 × 3 个预算（40/60/90）。

| 目标 | 策略 | 平均差 | sd |
|---|---|---|---|
| mean_r | `oracle_2lvl(mean_r)` | **+0.0069** | 0.0038 |
| mean_r | probe(dev 拟合) | −0.0072 | 0.0053 |
| mean_r | est_norm / random | −0.0111 / −0.0151 | — |
| frac≥0.7 | `oracle_2lvl(frac_ge_0.7)` | **+0.0402** | 0.0323 |
| frac≥0.7 | **probe(dev 拟合)** | **+0.0027** | 0.0160 |
| frac≥0.7 | `oracle_2lvl(mean_r)`（错配 oracle） | +0.0054 | 0.0207 |
| frac≥0.7 | random | −0.0271 | 0.0172 |

三点结论：
1. **两档策略族内确有 headroom**（frac≥0.7 上 +0.040，且集中在低预算：B=40 时 +0.075、
   60 时 +0.037、90 时 +0.010），mean_r 上 +0.0069 ± 0.0038 —— 与审阅重算一致；
2. **错配 oracle 会严重低估 headroom**（同一目标下 +0.0054 vs +0.0402）→ 必须目标匹配；
3. **但当前三个在线特征（half_split_r / delta_norm / cell_sd）没有捕捉到这份 headroom**：
   probe 在 frac≥0.7 上只有 **+0.0027 ± 0.0160**（≈0），在 mean_r 上为负。
   → **我上一轮 stage7 的 "+0.0206 信号"未能在更严格的管线中复现**，予以撤回。

## 4. 口径调整（采纳审阅意见）

- 审阅重算的配对 AUC 差 `0.0163 / 0.0216 / 0.0239`（均值 0.0206、样本 sd 0.0039）
  是**同一数据集上的重采样稳定性**，不是统计显著性；不再写"约 4σ"。
- **重新随机划分已分析过的扰动 ≠ 新的未使用测试集**：只能作内部验证；
  确认性结论必须保留真正未参与决策的数据或新任务。

## 5. 当前最稳的结论（已按最新证据收窄）

> 在两档（25→100）细胞分配族内，**存在可观的目标相关 headroom**（尾部目标 +0.04），
> 但**用当前三个在线统计量拟合的简单反馈规则尚未把它转化为收益**（+0.003 ± 0.016，n=3 划分）。
> 因此下一步不是训练 planner，而是：扩大划分数量、增加在线特征、
> 并接入下游终点，先确认"反馈信号是否可学"。

---

# 第四轮：扩到 10 个划分 + 接入下游终点 —— 终点选择的决定性证据

## 1. stage9：10 个划分（每策略 40 个配对观测，CI 为配对 95%）

在 MFBudgetEnv 上，dev/test 隔离、严格计费、剩余预算统一花掉（成本完全相等）：

| 目标 | 策略 | 平均差 vs uniform | 95% CI |
|---|---|---|---|
| mean_r | `oracle_mean_r` | **+0.0062** | [+0.0050, +0.0073] |
| mean_r | probe(dev 拟合, 7 特征) | −0.0078 | [−0.0094, −0.0061] |
| frac≥0.5 | `oracle_frac_ge_0.5` | **+0.0440** | [+0.0344, +0.0535] |
| frac≥0.5 | `oracle_frac_ge_0.7`（错配） | −0.0278 | [−0.0324, −0.0231] |
| frac≥0.7 | `oracle_frac_ge_0.7` | **+0.0402** | [+0.0315, +0.0489] |
| frac≥0.7 | **probe(dev 拟合)** | **+0.0125** | **[+0.0092, +0.0158]** |
| frac≥0.7 | est_norm / half_split_r | +0.0044 / +0.0032 | 均含 0 |
| frac≥0.7 | random | −0.0304 | [−0.0358, −0.0250] |

- **目标匹配的 oracle 在三个目标上都正**（+0.006 ~ +0.044）→ 两档族内确有 headroom；
- **错配 oracle 会反向**（frac≥0.5 上 oracle_frac_ge_0.7 = −0.0278）→ 再次确认必须目标匹配；
- **dev 拟合的 probe 在 frac≥0.7 上首次给出 CI 不含 0 的正效应 +0.0125**，
  但只有 oracle 的 **~31%**（"95%"的说法彻底作废）；mean_r 上仍为负；
- 10 个划分下随机分配一致为负 → 信号真实。

## 2. stage10：下游终点的决定性检验（同样 10 划分 × 4 预算）

问题：既然可靠度目标有 headroom，那**是否存在能改善下游终点（预测未见扰动响应）的分配**？
用"下游匹配 oracle"（在两档族内按**对下游 Pearson 的真实边际**排序）检验：

| 策略 | d(frac≥0.7) | d(downstream) |
|---|---|---|
| `oracle_downstream` | −0.0241 [−0.0294, −0.0188] | **+0.0069 [+0.0065, +0.0074]** |
| `oracle_frac_ge_0.7` | **+0.0402 [+0.0315, +0.0489]** | −0.0030 [−0.0035, −0.0025] |
| probe(dev 拟合) | +0.0105 [+0.0075, +0.0134] | −0.0025 [−0.0030, −0.0020] |
| random | −0.0304 | −0.0033 |
| uniform 绝对值 | 0.3138 | 0.2430 |

**跨策略的两目标相关系数 = −0.380**（成本相等校验通过）。

**结论（本轮最重要的发现）**：
1. **两个终点各自都有真实 headroom**：可靠度尾部 +0.040；下游预测 +0.0069（相对 +2.8%）。
2. **但两个目标在策略层面负相关（−0.38）**：把细胞集中给"高可靠度达标"的扰动，
   会**损害**对未见扰动的预测；反之亦然。
3. 因此 **"提高测量质量的扰动数"不能当作下游科学目标的代理**（审阅意见第 5 条得到定量确认）。
4. 当前 dev 拟合的 probe 只吃到了可靠度尾部 headroom 的 ~1/4，且对下游是负的。

## 3. 由此确定的下一步（终点必须先定，再谈 planner）

必须在动手训练 planner 之前确定科学终点，因为它决定优化对象：
- **若终点是下游预测**：要学的是"下游边际价值"预测器（用 dev 拟合 `marg` 的代理特征），
  而不是现在的"可靠度增益"预测器；本轮的 `oracle_downstream` 给出了该目标的上界 +0.0069。
- **若终点是发现型**（预先指定通路的真实响应命中）：需要另建终点并重新检验是否存在 headroom。
- 两个方向都要先回答"**该终点的边际价值能否从在线可观测特征预测**"，再决定是否值得引入 LLM。

**方法学注意**：下游终点目前用"先验核对齐的 kNN 预测器"近似管线中的 GEARS；
要与论文口径一致，最终需要用真实 GEARS 闭环复核（成本高）。这一点尚未做。

## 4. 本轮口径提醒（延续审阅意见）
- 上述 CI 是**同一数据集上 10 次划分重采样的配对 CI**，反映重采样稳定性，不是跨数据集的统计显著性；
- 10 个划分中 dev/test 互斥，但**整份数据都已被看过** → 确认性结论仍需未参与决策的新任务或新数据。
